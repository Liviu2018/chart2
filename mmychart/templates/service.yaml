apiVersion: v1
kind: Service
metadata:
  name: {{ include "mmychart.fullname" . }}
  labels:
    {{- include "mmychart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mmychart.selectorLabels" . | nindent 4 }}
