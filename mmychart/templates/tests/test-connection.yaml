apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mmychart.fullname" . }}-test-connection"
  labels:
    {{- include "mmychart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mmychart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
